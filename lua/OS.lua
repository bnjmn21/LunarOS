OS = {}
local btns = {}

local SETTINGS = {
    ["FunctionBar"] = {
        ["DisplayFunctionBar"] = true
    }
}

local function init()
    if SETTINGS["FunctionBar"]["DisplayFunctionBar"] then
        homebtn = {
        }

        table.insert(btns)
    end
end


local function draw()
    
end